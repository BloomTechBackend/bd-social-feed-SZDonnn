package com.bloomtech.socialfeed.validators;

public interface Validator {
    /**
     * Interface for object validation.
     * @param validationObject any object that needs validation
     */
    void validate(Object validationObject);
}
